
@model BlogdaGiovanna.Models.Post
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Detalhes</h2>

<div class="form-group">
    @Html.LabelFor(p => p.Title)
    <div>@Model.Title</div>
</div>
<div class="form-group">
    @Html.LabelFor(p => p.DateAdded) | @Html.LabelFor(p => p.DateAdded)
    <div>@Model.DateAdded.ToString("dd/MM/yyyy") | @Model.DateAdded</div>
</div>
<div class="form-group">
    @Html.LabelFor(p => p.PostingBody)
    <div>@Model.PostingBody</div>
</div>
@Html.ActionLink("Voltar", "Index", "Posts", null, new { @class = "btn btnc-primary" })
@Html.ActionLink("Edit", "Edit", "Posts", new { id = Model.id }, new { @class = "btn btnc-primary" })
@Html.ActionLink("Deletar", "Delete", "Posts", new { id = Model.id }, new { @class = "btn btnc-primary" })